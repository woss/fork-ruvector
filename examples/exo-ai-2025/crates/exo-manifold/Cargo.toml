[package]
name = "exo-manifold"
version = "0.1.0"
edition = "2021"

[dependencies]
exo-core = { path = "../exo-core" }
ndarray = "0.16"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
parking_lot = "0.12"

[dev-dependencies]
approx = "0.5"
